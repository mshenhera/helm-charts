apiVersion: v1
kind: Service
metadata:
  name: {{ include "tyk-gateway.fullname" . }}
  labels:
    {{- include "tyk-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tyk-gateway.selectorLabels" . | nindent 4 }}
